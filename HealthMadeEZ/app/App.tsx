import React from 'react';
import QRProvider from './QRContext'; // Import the default export

export default function App() {
  return (
    <QRProvider>
      {/* Your app's navigation or screens */}
    </QRProvider>
  );
}
